import { Router } from "express";
const devicesRouter = Router();

/**
 * /devices (for device registration [GET, POST, maybe PATCH?])
 * /devices/:id (for viewing sustainability facts of specific device)
 */

export default devicesRouter;